---
"@zerodev/react-ui": patch
---

feat: add `QrCode` — pill-style QR renderer (rounded finder patterns, `errorCorrectionLevel` and `eyeRadius` props), previously internal to the smart-routing-address package. Renders a fixed-size SVG: `<QrCode value={address} size={176} />`.
