import { LiveExample } from '../../../components/LiveExample'
import { CodeInput } from '../../../../packages/react-kit/src/auth/components/CodeInput'

# CodeInput

A multi-digit OTP/code input with auto-advance, paste support, and keyboard navigation.

## Import

```tsx
import { CodeInput } from '@zerodev/wallet-react-kit'
```

## Usage

```tsx
<CodeInput
  length={6}
  onComplete={(code) => verifyOTP(code)}
/>
```

## Live Example

<LiveExample>
  <div style={{ display: 'flex', flexDirection: 'column', gap: 16, alignItems: 'center' }}>
    <div>
      <p style={{ fontSize: 13, color: '#666', marginBottom: 8 }}>Default (6 digits)</p>
      <CodeInput onComplete={(code) => alert(`Code: ${code}`)} />
    </div>
    <div>
      <p style={{ fontSize: 13, color: '#666', marginBottom: 8 }}>4 digits</p>
      <CodeInput length={4} />
    </div>
    <div>
      <p style={{ fontSize: 13, color: '#666', marginBottom: 8 }}>Error state</p>
      <CodeInput error />
    </div>
    <div>
      <p style={{ fontSize: 13, color: '#666', marginBottom: 8 }}>Disabled</p>
      <CodeInput disabled />
    </div>
  </div>
</LiveExample>
